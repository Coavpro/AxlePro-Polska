<section class="product-hero">
  <div class="container">
      <div class="d-table">
          <div class="d-table-cell center">
              <div class="hero-copy">
                  <h1 class="h1-title">AxlePro-Polska</h1>
                  <p>Twoim zaufanym dostawcy osi i zawieszeń do ciężarówek! 
                    Naszym celem jest zapewnienie najwyższej jakości rozwiązań dla transportu ciężarowego. 
                    Oferujemy niezawodne części, które zwiększają bezpieczeństwo i wydajność Twojej floty.</p>
              </div>
          </div>
      </div>
  </div>
</section>

<style lang="scss">
.product-hero {
padding: 20px 0; // Reduce padding to minimize distance
.container {
  position: relative;
  .d-table {
    height: 200px; // Adjust height to reduce space
  }
}
.hero-media {
  padding: 5px;
  border-radius: 48px;
}
.hero-copy {
  h1, p {
    max-width: 600px;
    margin: 1px auto; // Reduce margin to minimize distance
  }
  h1 {
    color: red;
  }
}
.hero-media {
  img {
    position: absolute;
    width: 160px;
    &:nth-child(1) {
      top: 0;
      left: -100px;
    }
    &:nth-child(2) {
      top: 0;
      right: -100px;
    }
    &:nth-child(3) {
      top: 250px;
      left: 0;
    }
    &:nth-child(4) {
      top: 250px;
      right: 0;
    }
    &:nth-child(5) {
      bottom: -20px;
      left: 50%;
      transform: translateX(-50%);
    }
  }
  @media (max-width: 700px) {
    img {
      &:nth-child(2), &:nth-child(3),
      &:nth-child(4) {
        display: none;
      }
    }
  }
}
}
</style>